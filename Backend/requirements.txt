# Core dependencies (recommended for full functionality)
pandas>=2.0.0
PyYAML>=6.0
python-docx>=1.0.0
PyPDF2>=3.0.0

# Optional: For better Excel support
openpyxl>=3.1.0  # Required for pandas to read .xlsx files

# Note: The classifier will work without these dependencies,
# but will have limited support for certain file types:
# - Without pandas: CSV/Excel parsing will be basic
# - Without PyYAML: YAML files won't be supported
# - Without python-docx: DOCX files won't be supported
# - Without PyPDF2: PDF files won't be supported

